import { Space_Grotesk } from 'next/font/google'

// Export a shared font instance so UI components can reuse the same className
export const spaceGrotesk = Space_Grotesk({ subsets: ['latin'] })

export default spaceGrotesk
